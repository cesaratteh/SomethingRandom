package game.action.handlers.utils;

/**
 * Handles mapping settlements
 * Should have two main functions:
 *      1- Given a MapSpot, return a list of map spots that belong to the same settlement
 *      2- Given a MapSpot in a settlement, return a list of map spots that can be expanded to
 *
 * Should use DFS or BFS
 */
public class SettlmentMapper {
}
